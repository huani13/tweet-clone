### Repository for backend system of tweet api clone

To run:
- go to backend directory
- run `pip install -r requirements.txt`
- set environment variables of `DATABASE_URL`
- run `fastpi dev main.py`
